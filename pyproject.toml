[tool.poetry]
name = "mocon"
version = "0.1.0"
description = "mocap data converter"
authors = ["ShiHan Wan <shihanwan@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
